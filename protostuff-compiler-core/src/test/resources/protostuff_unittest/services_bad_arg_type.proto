syntax = "proto3";

package protostuff_unittest;

service SearchService {
    // Only messages are valig arguments for rpc methods
    rpc search(Enum) returns (SearchResponse);
}

enum Enum {

}

message SearchResponse {

}